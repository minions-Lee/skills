# GitHub KB - AI 工具知识库使用手册

版本：v1.0
作者：goodniuniu
仓库：https://github.com/goodniuniu/github-kb

---

## 目录

1. [项目简介](#1-项目简介)
2. [快速开始](#2-快速开始)
3. [安装指南](#3-安装指南)
4. [基础使用](#4-基础使用)
5. [高级功能](#5-高级功能)
6. [使用场景](#6-使用场景)
7. [维护指南](#7-维护指南)
8. [故障排除](#8-故障排除)
9. [最佳实践](#9-最佳实践)
10. [FAQ](#10-faq)

---

## 1. 项目简介

### 1.1 什么是 GitHub KB？

GitHub KB 是一个基于 **gh CLI** + **Claude Code** 的 AI 仓库知识库管理系统。它帮助您：

- 📚 管理本地 AI 相关的 GitHub 仓库
- 🔍 让 Claude 深度理解 AI 项目架构
- 💡 基于仓库分析，提供 AI 工具/产品构建建议
- 🎯 智能匹配技术栈，快速实现技术选型

### 1.2 核心价值

**传统方式：**
```
想到 AI 想法 → Google 搜索 → 阅读文档 → 对比多个项目 → 尝试集成
（耗时数小时到数天）
```

**使用 GitHub KB：**
```
想到 AI 想法 → 问 Claude → 基于本地仓库分析 → 获得技术方案
（几分钟内完成）
```

### 1.3 适用人群

- ✅ AI 工具开发者
- ✅ 全栈工程师
- ✅ 创业者（MVP 开发）
- ✅ AI 领域研究者
- ✅ 想快速构建 AI 应用的开发者

### 1.4 项目结构

```
github-kb/
├── .claude/
│   └── settings.local.json           # Claude Code 配置
├── skills/
│   └── github-kb/
│       ├── SKILL.md                  # 技能说明（Claude 读取）
│       ├── init-repos.sh             # 初始化脚本
│       ├── install-gh.sh             # gh CLI 安装脚本
│       └── OPTIMIZATION.md           # 优化说明文档
├── CLAUDE.md                          # 知识库目录（核心）
├── README.md                          # 项目说明
├── USER_GUIDE.md                      # 使用手册（本文件）
├── SETUP.md                           # 快速设置指南
└── .gitignore
```

---

## 2. 快速开始

### 2.1 5 分钟快速体验

```bash
# 1. 克隆项目
git clone https://github.com/goodniuniu/github-kb.git
cd github-kb

# 2. 安装 gh CLI
bash skills/github-kb/install-gh.sh

# 3. 认证 GitHub
gh auth login

# 4. 初始化知识库（克隆 AI 仓库）
bash skills/github-kb/init-repos.sh

# 5. 开始使用！
# 在项目目录下用 Claude Code 提问
```

### 2.2 第一个问题

向 Claude 提问：

> "请分析 clawdbot 和 open-interpreter 的架构设计，对比它们的实现方式，告诉我各自的优势。"

Claude 会：
1. 读取 `~/github/` 下的仓库代码
2. 分析架构和实现
3. 对比优缺点
4. 给出建议

---

## 3. 安装指南

### 3.1 系统要求

- **操作系统**：Linux、macOS、Windows (WSL)
- **Git**：2.20+
- **Bash**：4.0+
- **磁盘空间**：至少 500MB（用于 AI 仓库）
- **网络**：能访问 GitHub

### 3.2 安装 gh CLI

#### 方式 1：自动安装（推荐）

```bash
bash skills/github-kb/install-gh.sh
```

#### 方式 2：手动安装

**Linux (Debian/Ubuntu):**
```bash
curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | sudo dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg
sudo chmod go+r /usr/share/keyrings/githubcli-archive-keyring.gpg
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null
sudo apt update
sudo apt install gh
```

**macOS:**
```bash
brew install gh
```

**Windows (WSL):**
```bash
# 使用 Linux 命令
```

### 3.3 认证 GitHub

```bash
gh auth login
```

按照提示操作：
1. 选择 `GitHub.com`
2. 选择 `HTTPS`
3. 选择登录方式（浏览器或 token）
4. 完成认证

验证：
```bash
gh auth status
```

应该显示：
```
✓ Logged in to github.com account <your-username>
```

### 3.4 配置知识库路径

默认路径：`~/github`

修改方式：

编辑 `CLAUDE.md`：
```markdown
默认克隆路径：`/your/custom/path`
```

编辑 `init-repos.sh`：
```bash
KB_DIR="${GITHUB_KB_DIR:-/your/custom/path}"
```

---

## 4. 基础使用

### 4.1 初始化知识库

#### 自动初始化（推荐）

```bash
cd github-kb
bash skills/github-kb/init-repos.sh
```

**脚本会自动：**
1. 检查网络和 Git
2. 克隆推荐的 AI 仓库（最多 3 个并行）
3. 失败自动重试（最多 3 次）
4. 显示详细进度和摘要

**默认仓库列表：**
- clawdbot - 个人 AI 助手
- open-interpreter - 代码执行助手
- oh-my-opencode - OpenCode 增强版
- langchain - LLM 开发框架
- transformers - Hugging Face 库
- llama.cpp - 本地 LLM 推理

#### 手动克隆单个仓库

```bash
cd ~/github
gh repo clone clawdbot/clawdbot
```

或使用 git：
```bash
git clone --depth 1 https://github.com/clawdbot/clawdbot.git
```

### 4.2 向 Claude 提问

#### 示例 1：了解仓库

> "clawdbot 是什么项目？它的核心功能有哪些？"

Claude 会：
1. 读取仓库的 README
2. 分析代码结构
3. 总结核心功能

#### 示例 2：架构分析

> "请分析 open-interpreter 的架构设计，它是如何实现代码执行的？"

Claude 会：
1. 研究源代码结构
2. 识别关键模块
3. 解释架构设计

#### 示例 3：技术选型

> "我想做一个类似 ChatGPT 的网页应用，支持流式输出。基于我的知识库，有什么推荐方案？"

Claude 会：
1. 匹配相关仓库（clawdbot、langchain）
2. 分析技术栈
3. 推荐最佳方案
4. 提供实现步骤

#### 示例 4：对比分析

> "对比 clawdbot 和 open-interpreter，在设计理念和技术实现上有什么不同？"

Claude 会：
1. 深度分析两个仓库
2. 对比架构设计
3. 总结优缺点
4. 给出选型建议

### 4.3 添加新仓库到知识库

#### 步骤 1：克隆仓库

```bash
cd ~/github
gh repo clone <owner>/<repo-name>
```

#### 步骤 2：更新 CLAUDE.md

在 `CLAUDE.md` 中添加：

```markdown
## 分类名称

### [repo-name](/repo-name)
项目描述 - 一句话说明功能
核心技术栈：技术1、技术2
适用场景：场景1、场景2
```

**示例：**

```markdown
## AI & Assistants

### [autogen](/autogen)
微软的多智能体框架 - 支持多智能体协作、对话编排、工具调用
核心技术栈：Python、AsyncIO、OpenAI API
适用场景：多智能体系统、AI Agent 协作、复杂任务自动化
```

#### 步骤 3：让 Claude 知道

> "我刚添加了 autogen 仓库到知识库，请了解一下。"

Claude 会读取并理解新添加的仓库。

### 4.4 查看知识库

> "我的知识库中有哪些项目？按分类列出。"

Claude 会读取 `CLAUDE.md` 并列出所有项目。

---

## 5. 高级功能

### 5.1 自定义配置

#### 修改并发数

编辑 `init-repos.sh`：
```bash
MAX_PARALLEL=5  # 增加到 5 个并行
```

#### 修改重试次数

```bash
MAX_ATTEMPTS=5  # 增加到 5 次重试
```

#### 修改超时时间

```bash
CLONE_TIMEOUT=600  # 增加到 10 分钟
```

### 5.2 批量管理仓库

#### 更新所有仓库

```bash
cd ~/github
for dir in */; do
    if [ -d "$dir/.git" ]; then
        echo "Updating $dir..."
        cd "$dir"
        git pull --quiet
        cd ..
    fi
done
```

#### 查看仓库状态

```bash
cd ~/github
for dir in */; do
    if [ -d "$dir/.git" ]; then
        cd "$dir"
        echo -n "$dir: "
        git status -sb | head -1
        cd ..
    fi
done
```

#### 统计仓库大小

```bash
cd ~/github
du -sh */ | sort -h
```

### 5.3 搜索功能

#### 搜索 GitHub 仓库

> "搜索 GitHub 上关于 AI Agent 的项目，stars > 1000"

Claude 会使用 gh 搜索并返回结果。

#### 搜索仓库内容

> "在我的知识库中，搜索包含 'RAG' 的项目"

Claude 会搜索所有仓库的 README 和文档。

### 5.4 导出知识库

#### 导出为 Markdown

```bash
cat CLAUDE.md > knowledge-base-$(date +%Y%m%d).md
```

#### 导出仓库列表

```bash
cd ~/github
ls -1 > repo-list.txt
```

### 5.5 多服务器同步

#### 场景：在多个服务器上使用

**服务器 A：**
```bash
# 克隆 github-kb
git clone https://github.com/goodniuniu/github-kb.git
cd github-kb

# 初始化
bash skills/github-kb/init-repos.sh

# 修改并推送
git add CLAUDE.md
git commit -m "Add new repositories"
git push
```

**服务器 B：**
```bash
# 拉取最新配置
git pull
```

---

## 6. 使用场景

### 6.1 场景 1：快速技术选型

**需求：** 想构建一个 AI 客服系统

**提问：**
> "我想构建一个 AI 客服系统，需要支持：
> - 多渠道接入（网页、微信、Slack）
> - 知识库检索
> - 流式对话
>
> 基于我的知识库，推荐技术方案。"

**Claude 的回答：**
1. 推荐使用 clawdbot（多消息渠道支持）
2. 推荐使用 langchain（RAG 能力）
3. 提供架构设计
4. 给出实现步骤

### 6.2 场景 2：学习 AI 项目架构

**需求：** 想了解 AI 编码代理的实现

**提问：**
> "请深入研究 opencode 和 open-interpreter：
> 1. 分析它们的整体架构
> 2. 对比代码执行机制
> 3. 总结设计模式
> 4. 我应该如何借鉴？"

**Claude 的回答：**
1. 分析 opencode 的 TypeScript 架构
2. 分析 open-interpreter 的 Python 沙箱
3. 对比两种方案的优劣
4. 给出学习建议

### 6.3 场景 3：MVP 快速开发

**需求：** 2 周内上线 AI 文档助手

**提问：**
> "我需要 2 周内开发一个 AI 文档助手：
> - 上传文档（PDF、Word）
> - AI 问答
> - 引用来源
>
> 推荐技术栈和实现方案。"

**Claude 的回答：**
1. 推荐 langchain（文档处理）
2. 推荐 llama.cpp（本地部署）
3. 提供技术架构
4. 给出 2 周开发计划

### 6.4 场景 4：性能优化

**需求：** 优化现有 AI 应用

**提问：**
> "我的 AI 应用响应慢（3-5 秒），基于知识库中的项目：
> - 分析可能的瓶颈
> - 提供优化方案
> - 参考最佳实践"

**Claude 的回答：**
1. 分析常见性能问题
2. 参考各项目的优化方案
3. 提供具体优化建议

### 6.5 场景 5：产品决策

**需求：** 选择技术路线

**提问：**
> "我要做一个企业 AI 助手：
> - 要求本地部署（数据安全）
> - 支持 100+ 并发
> - 预算有限
>
> 应该选择哪个方案？"
> - A: 基于 OpenAI API
> - B: 基于 llama.cpp
> - C: 基于 clawdbot

**Claude 的回答：**
1. 分析各方案的成本
2. 评估技术可行性
3. 推荐最佳方案
4. 给出风险提示

---

## 7. 维护指南

### 7.1 定期更新仓库

**建议频率：** 每周一次

```bash
cd ~/github

# 更新所有仓库
for dir in */; do
    if [ -d "$dir/.git" ]; then
        echo "Updating $dir..."
        cd "$dir"
        git pull --quiet --ff-only
        cd ..
    fi
done
```

### 7.2 清理无用仓库

```bash
cd ~/github

# 查看所有仓库
ls -lh

# 删除不需要的仓库
rm -rf <repo-name>

# 更新 CLAUDE.md，移除对应条目
```

### 7.3 备份知识库

**方式 1：备份配置**

```bash
cd github-kb
tar -czf github-kb-backup-$(date +%Y%m%d).tar.gz CLAUDE.md .claude/
```

**方式 2：Git 追踪**

```bash
cd github-kb
git add CLAUDE.md
git commit -m "Update knowledge base catalog"
git push
```

### 7.4 监控磁盘空间

```bash
# 查看总大小
du -sh ~/github

# 查看各仓库大小
du -sh ~/github/* | sort -h
```

如果空间不足：
```bash
# 使用浅克隆重新克隆
git clone --depth 1 <repo-url>
```

### 7.5 更新 CLAUDE.md 模板

保持 `CLAUDE.md` 的更新：

```markdown
# Claude Code 知识库

本目录包含 X 个 AI 相关 GitHub 项目，涵盖...领域。

默认克隆路径：`~/github`

---

## 分类名称

### [repo-name](/repo-path)
项目描述 - 一句话说明
核心技术栈：技术1、技术2、技术3
适用场景：场景1、场景2

---
```

---

## 8. 故障排除

### 8.1 常见问题

#### 问题 1：gh 命令找不到

**错误：**
```
bash: gh: command not found
```

**解决：**
```bash
bash skills/github-kb/install-gh.sh
```

#### 问题 2：认证失败

**错误：**
```
GraphQL: Could not resolve to a Repository
```

**解决：**
```bash
gh auth logout
gh auth login
```

#### 问题 3：克隆超时

**错误：**
```
fatal: unable to access 'https://github.com/...': Connection timeout
```

**解决：**
1. 检查网络连接
2. 增加超时时间（编辑 `init-repos.sh`）
3. 使用代理

#### 问题 4：磁盘空间不足

**错误：**
```
No space left on device
```

**解决：**
```bash
# 清理仓库 .git 目录
cd ~/github
for dir in */; do
    cd "$dir"
    git gc --aggressive --prune=now
    cd ..
done
```

或使用浅克隆：
```bash
rm -rf <repo>
git clone --depth 1 <repo-url>
```

#### 问题 5：Claude 无法读取仓库

**可能原因：**
- 仓库路径不正确
- 仓库未完成克隆
- 权限问题

**解决：**
```bash
# 检查仓库
cd ~/github
ls -la <repo>/

# 检查权限
chmod -R +r ~/github/
```

### 8.2 网络问题

#### 检查 GitHub 连接

```bash
ping github.com
```

#### 检查 DNS 解析

```bash
nslookup github.com
```

#### 使用代理

```bash
# 设置 Git 代理
git config --global http.proxy http://proxy.example.com:8080
git config --global https.proxy https://proxy.example.com:8080

# 取消代理
git config --global --unset http.proxy
git config --global --unset https.proxy
```

### 8.3 性能问题

#### Claude 响应慢

**可能原因：**
- 仓库太大
- 同时分析太多仓库
- 网络带宽不足

**解决：**
1. 使用 `--depth 1` 浅克隆
2. 只保留必要的仓库
3. 针对性提问，避免模糊问题

#### 克隆太慢

**解决：**
1. 增加并行数：`MAX_PARALLEL=5`
2. 使用浅克隆：`--depth 1`
3. 更换网络环境

### 8.4 调试模式

#### 启用详细输出

```bash
bash -x skills/github-kb/init-repos.sh
```

#### 查看 Git 日志

```bash
export GIT_TRACE=1
export GIT_CURL_VERBOSE=1
gh repo clone <repo>
```

---

## 9. 最佳实践

### 9.1 仓库管理

**✅ 推荐做法：**
- 定期更新仓库（每周）
- 使用浅克隆节省空间
- 分类组织仓库
- 及时清理无用仓库

**❌ 避免：**
- 克隆太多不相关的仓库
- 长期不更新
- 忽略磁盘空间

### 9.2 提问技巧

**✅ 好的问题：**

1. **具体明确**
   > "分析 clawdbot 的消息路由机制，它是如何实现多渠道适配的？"

2. **有上下文**
   > "基于我知识库中的 langchain 和 clawdbot，设计一个 RAG 客服系统。"

3. **有目标**
   > "我想 2 周内开发 AI 文档助手，推荐技术栈和开发计划。"

**❌ 避免的问题：**

1. **太模糊**
   > "AI 项目有哪些？" （太宽泛）

2. **无上下文**
   > "怎么用 langchain？" （缺少场景）

3. **超出范围**
   > "如何用 Python 从零开发一个 LLM？" （应该问如何使用现有框架）

### 9.3 知识库组织

**按层次组织：**

```markdown
## 一级分类（大领域）
## 二级分类（子领域）
### 具体项目
```

**示例：**

```markdown
## LLM Frameworks
### RAG Tools
#### [langchain](/langchain)
#### [llama-index](/llama-index)

### Model Serving
#### [vllm](/vllm)
#### [text-generation-webui](/text-generation-webui)
```

### 9.4 协作建议

**团队使用：**

1. **统一知识库路径**
   ```bash
   export GITHUB_KB_DIR=/shared/github-kb
   ```

2. **定期同步**
   ```bash
   git pull
   bash skills/github-kb/init-repos.sh
   ```

3. **贡献新仓库**
   ```bash
   # 添加仓库
   gh repo clone <new-repo>

   # 更新 CLAUDE.md
   git add CLAUDE.md
   git commit -m "Add <new-repo> to knowledge base"
   git push
   ```

### 9.5 安全建议

**敏感信息：**

1. **不要克隆：**
   - 包含 API keys 的仓库
   - 包含密码的仓库
   - 私有密钥

2. **检查仓库：**
   ```bash
   # 克隆前检查
   gh repo view <repo>
   ```

3. **使用 .gitignore：**
   ```bash
   # 排除敏感文件
   echo "*.env" >> .gitignore
   echo "secrets/" >> .gitignore
   ```

---

## 10. FAQ

### Q1: 可以使用私有仓库吗？

**A:** 可以。确保：
1. 已认证 GitHub
2. 有访问权限
3. 使用 `gh` 命令克隆

```bash
gh repo clone <private-repo>
```

### Q2: 可以在其他服务器使用吗？

**A:** 可以。步骤：
```bash
git clone https://github.com/goodniuniu/github-kb.git
cd github-kb
bash skills/github-kb/install-gh.sh
gh auth login
bash skills/github-kb/init-repos.sh
```

### Q3: 如何添加自己的项目？

**A:**
1. 推送到 GitHub
2. 克隆到 `~/github`
3. 更新 `CLAUDE.md`

### Q4: 知识库可以多人共享吗？

**A:** 可以。方式：
1. Fork 项目
2. 修改 `CLAUDE.md`
3. 提交 PR

或直接：
```bash
git clone <your-fork>
# 修改
git push
```

### Q5: 如何选择要克隆的仓库？

**A:** 建议：
1. 从推荐仓库开始
2. 根据兴趣领域添加
3. 定期清理不用的
4. 保持 10-20 个活跃仓库

### Q6: 克隆后需要更新吗？

**A:** 需要。建议：
- 频繁使用：每周更新
- 偶尔使用：每月更新
- 参考性质：按需更新

### Q7: 可以离线使用吗？

**A:** 部分可以：
- ✅ 仓库代码分析（完全离线）
- ✅ 架构研究（完全离线）
- ❌ GitHub 搜索（需要网络）
- ❌ 克隆新仓库（需要网络）

### Q8: 磁盘空间不够怎么办？

**A:** 解决方案：
1. 使用浅克隆：`--depth 1`
2. 删除 `.git` 目录（不需要更新时）
3. 删除不用的仓库
4. 使用外部存储

### Q9: Claude 无法理解某些仓库？

**A:** 可能原因：
1. 仓库太大（拆分提问）
2. 代码太复杂（提供上下文）
3. 文档不完整（先阅读 README）

### Q10: 如何定制分类？

**A:** 编辑 `CLAUDE.md`：

```markdown
## Your Custom Category

### [repo](/repo)
Description
```

---

## 附录

### A. 推荐仓库列表

**AI 助手：**
- clawdbot - 个人 AI 助手
- open-interpreter - 代码执行助手
- AutoGen - 多智能体框架

**AI 编码：**
- opencode - AI 编码代理
- oh-my-opencode - OpenCode 增强
- Cursor - AI 编辑器

**LLM 框架：**
- langchain - LLM 应用框架
- llama-index - 数据框架
- semantic-kernel - 微软框架

**本地部署：**
- llama.cpp - 本地推理
- ollama - 模型管理
- vllm - 高性能推理

**RAG 工具：**
- langchain - RAG 框架
- llamaindex - 数据索引
- chroma - 向量数据库

### B. 快速命令参考

```bash
# 初始化
bash skills/github-kb/init-repos.sh

# 克隆仓库
gh repo clone <repo>

# 更新仓库
cd ~/github && for d in */; do cd $d && git pull && cd ..; done

# 查看大小
du -sh ~/github/*

# 搜索仓库
gh search repos <query>

# 查看仓库信息
gh repo view <repo>
```

### C. 配置文件位置

- **知识库路径**：`~/github`
- **配置文件**：`.claude/settings.local.json`
- **技能文件**：`skills/github-kb/SKILL.md`
- **目录文件**：`CLAUDE.md`

### D. 相关资源

- **GitHub**：https://github.com/goodniuniu/github-kb
- **gh CLI**：https://cli.github.com/
- **Claude Code**：https://claude.ai/code

---

## 更新日志

**v1.0 (2026-01-27)**
- 初始版本
- 完整使用手册
- 包含 10 个章节和 FAQ

---

**文档维护**：goodniuniu
**最后更新**：2026-01-27
**反馈渠道**：https://github.com/goodniuniu/github-kb/issues
